import { FilterByDietPipe } from './filter-by-diet.pipe';

describe('FilterByDietPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDietPipe();
    expect(pipe).toBeTruthy();
  });
});
